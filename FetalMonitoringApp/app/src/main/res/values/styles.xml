<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">#13000000</item>
        <item name="colorPrimaryDark">#13000000</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Drawer menu style -->
    <style name="NavigationViewStyle">
        <item name="android:textSize">22sp</item>
        <item name="android:fontFamily">@string/default_string2</item>
        <item name="android:padding">2dp</item>
    </style>

    <!-- Splash screen theme -->
    <style name="SplashScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#00FFFFFF</item>
        <item name="colorPrimaryDark">#00FFFFFF</item>
        <item name="colorAccent">#00FFFFFF</item>
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Fab button theme -->
    <style name="FabButtonTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Add attributes here -->
        <item name="textAppearanceButton">@style/AppTextAppearance.Button</item>
    </style>

    <!--To fix rendering in preview -->
    <style name="AppTextAppearance.Button" parent="TextAppearance.MaterialComponents.Button">
        <item name="android:textAllCaps">true</item>
    </style>

</resources>
